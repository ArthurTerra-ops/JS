/*function contar()
{
var NumeroI = document.getElementByName("InumeroI").Value
var NumeroF = document.getElementByName("InumeroF").Value
var NumeroQ = document.getElementByName("InumeroQ").Value

for(NumeroI; NumeroI == NumeroF ; NumeroI = NumeroQ + NumeroI )
    {
        alert(NumeroI)

    }

} */


/*
var NumeroI = document.getElementByName("InumeroI").Value
var NumeroF = document.getElementByName("InumeroF").Value
var NumeroQ = document.getElementByName("InumeroQ").Value*/

var NumeroI = 20
var NumeroF = 50
var NumeroQ = 3
for(X = 20; X <= 50 ; NumeroI = NumeroQ + X )
    {
        console.log(X)
        

    }